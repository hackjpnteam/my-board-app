version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
  environmentVariables:
    MONGODB_URI: mongodb+srv://username:hikarutomura@cluster.mongodb.net/board-app?retryWrites=true&w=majority 