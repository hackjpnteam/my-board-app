version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - npm ci --legacy-peer-deps --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
      - .npm-cache/**/* 